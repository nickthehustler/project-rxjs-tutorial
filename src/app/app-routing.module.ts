import {Routes } from '@angular/router';
import { CounterButtonsComponent } from './counter-buttons/counter-buttons.component';

export const appRoutes: Routes = [
    {path: 'counter', component: CounterButtonsComponent}
  ];